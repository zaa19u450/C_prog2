# Условие

В виде списка представлены перечисленные по строкам ненулевые элементы целочисленной матрицы 𝑀.
Обратите внимание, что специально не указываются размеры
матрицы — представление в виде списка ненулевых элементов позволяет достраивать
матрицу до матрицы любого размера заполнением нулями.

#### Требуется:

- (a) Реализовать подпрограмму сложения матриц.

- (b) Реализовать подпрограмму умножения матриц.

- (c) Реализовать подпрограмму удаления из матрицы строки с максимальным элементом по матрице.

#### Правила взаимодействия:

- (a) При старте программы пользователь вводит одно из четырёх слов: out, add, mul,
lin, а далее - одну или две матрицы, в зависимости от команды.

- (b) Матрица вводится по строкам, причём сначала печатаются число строк и число
столбцов.

- (c) При выводе матрицы на экран выводить поля элементов списка через пробел
при обходе по строкам, как в примере.


## Негативные тесты:

### Ошибки ввода
- 01 - несушествующая команда (abc);
- 02 - несушествующая команда, похожая на существующую (out.);
- 03 - отрицательное m;
- 04 - нулевое n;
- 05 - нечисловое m;
- 06 - дробное n;
- 07 - недостаточно элементов матрицы (<m*n);
- 08 - нечисловой элемент матрицы;
- 09 - нецелочисленный элемент матрицы;
- 10 - все элементы матрицы нулевые;
- 11 - для команды add не дана вторая матрица;
- 12 - для команды mul не дана вторая матрица;

### Ошибки в вычислениях
- 13 - в результате сложения матриц получилась пустая матрица;
- 14 - в результате умножения матриц получилась пустая матрица;
- 15 - в результате удаления из матрицы строки с максимальным элементом по матрице получилась пустая матрица;


## Позитивные тесты:

### out
- 01 - в исходной матрице всего 1 элемент;
- 02 - в исходной матрице несколько элементов (различные случаи - пустые строки/столбцы, один/несколько элементов в строке/столбце);
- 08 - тест из примеров;

### add
- 03 - обший тест: размерности исходных матриц не совпадают, в исходных матрицах есть пустые строки/столбцы, 
в полученной матрице некоторые элементы получены простым копированием из первой/второй матрицы, сложением элементов первой/второй матрицы,
обнулились в результате сложения;
- 09 - тест из примеров;

### mul
- 04 - обший тест: n1!=m2, в исходных матрицах есть пустые строки/столбцы, 
в полученной матрице некоторые элементы получены умножением целых строк и столбцов, умножением одной пары элементов,
обнулились в результате сложения результатов умножения соответствующих пар элементов;
- 10 - тест из примеров;

### lin
- 05 - удаляется первая ненулевая строка;
- 06 - удаляется срединная ненулевая строка (заодно проверяется поведение при нескольких строках с максимальным элементом);
- 07 - удаляется последняя ненулевая строка;
